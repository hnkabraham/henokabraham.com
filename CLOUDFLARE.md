# Deploy to your Cloudflare account

This deployment uses the latest project in `Developer/henokabraham.com`, built with Vinext and the Cloudflare Vite plugin. The Worker is named `henokabraham-com`. The primary hostname is `henokabraham.com`, configured as a Cloudflare Worker Custom Domain. The `henokabraham-com.henok37.workers.dev` address is also enabled.

## Credentials

Create a custom API token at https://dash.cloudflare.com/profile/api-tokens with:

- Account → Workers Scripts → Edit.
- Account → Account Settings → Read.
- Account Resources → Include → only the account that should host this site.

The live services use a dedicated Workers KV namespace. Add Workers KV Storage permissions when managing or seeding it. Turnstile widget management needs Turnstile Edit; the setup of Web Analytics and Email Routing needs their corresponding account/zone permissions. Routine deployment preserves the existing resources and Turnstile secret. Custom hourly performance summaries use a dedicated D1 database (D1 Edit permission for setup); there are no R2 bindings; scenery remains on Workers Static Assets.

Add the token and the account's 32-character ID to `.env.cloudflare.local`. That file is ignored by Git and is created with file permissions restricted to me. Do not add the token to source code or Vite public environment variables. The deployment script supplies credentials to Wrangler after building; they are not application secrets or Worker bindings.

```dotenv
CLOUDFLARE_API_TOKEN=your_scoped_token
CLOUDFLARE_ACCOUNT_ID=your_account_id
CONTACT_TO=you@example.com
```

The script also accepts a Global API Key through `CLOUDFLARE_API_KEY` plus `CLOUDFLARE_EMAIL`. Global keys have full account access; user API tokens allow the narrower permissions above. Global keys must not be passed as `CLOUDFLARE_API_TOKEN`. Credentials supplied through the command environment are not saved by the script. `CONTACT_TO` is not a Cloudflare credential, but it travels the same way: it is a build-time input read from this file, not a literal in the repository, so `vite.config.ts` only wires up the contact form's `send_email` binding when it is set. A checkout without it (a fork, CI) builds and deploys fine; the contact form just reports itself unavailable.

Use a token expiration appropriate to the deployment session. The existing Sites-hosted preview remains available independently.

## Validate without deploying

```sh
npm run check:cloudflare
```

This builds the current source and runs Wrangler's deployment dry run. It requires no API token and does not upload the Worker or assets.

## Deploy

```sh
npm run deploy:cloudflare
```

The command validates credentials, builds the current source, checks the expected Worker name and bindings, then uploads the Worker and static assets. Wrangler prints the resulting URL. Cloudflare's `workers.dev` deployment is publicly reachable; this is the public personal website target, not the private Sites preview.

Before an initial upload, check for an existing Worker with this name in the selected account. After deployment, check the homepage and representative model, tile and atmosphere URLs. `henokabraham.com` is retained in the Cloudflare-target Vite configuration, so subsequent deployments preserve the custom domain. A scoped token used to manage domain routing should also include Zone → Zone → Read and Zone → Workers Routes → Edit, restricted to the `henokabraham.com` zone.

The original `npm run build` command retains Sites support. `npm run build:cloudflare` skips the Sites metadata plugin and enables the direct Cloudflare target; use the deployment command to ensure the correct output is built before upload.

References:

- https://developers.cloudflare.com/workers/ci-cd/external-cicd/github-actions/
- https://developers.cloudflare.com/workers/wrangler/system-environment-variables/
- https://developers.cloudflare.com/workers/configuration/routing/custom-domains/

## Live deployment

- Primary URL: https://henokabraham.com
- Workers URL: https://henokabraham-com.henok37.workers.dev
- Worker: `henokabraham-com`
- Cloudflare version: `a7e02827-9d66-4716-bd8c-75a269d23694`
- Previous service verification: homepage and both live-data/config endpoints return HTTP 200; five sampled versioned 3D assets have immutable cache headers and match local SHA-256 hashes. Live origin, Turnstile and metric validation reject invalid requests. Cloudflare confirmed delivery of the test email to my inbox. D1 contains live scene readiness and frame-rate summaries; the 15-minute cron is registered.
- Current release: transparent airline logos with representative brand colors and clear spacing, source `035e597` (September 12, 2026); scene assets `c7512aa4f3f205f9`.
- Current validation: TypeScript, summary/asset/privacy checks, the production build and deployment passed. The live homepage returns HTTP 200 with all 14 airline logos; the stylesheet and all 14 logo files match local SHA-256 hashes. American’s three-color SVG and the isolated Frontier mark were inspected as standalone assets. Airline totals, country order and individual-flight exclusions remain unchanged. This release did not include browser QA.
- Deployment credentials are read from my own, git-ignored `.env.cloudflare.local`. They are excluded from the application build and Worker bindings.

## Zone settings that affect the opening

- **Bot Fight Mode and JavaScript Detections: off.** With them on, Cloudflare injects `/cdn-cgi/challenge-platform/scripts/jsd/main.js` into every page; on 2026-09-10 it was the largest main-thread cost before the scene appeared (340 ms of 1.4 s) and it can challenge visitors that look automated. The AI-crawler block injects nothing and stays on.
- **Smart Tiered Cache: on**, so the first visitor at a Cloudflare location is served from an upper-tier cache rather than storage.
- **Early Hints: on.** `worker.ts` adds a `Link: rel=preload; as=image` header for the opening sky to the document response; Cloudflare replays them as 103 responses on later requests for the same URL.

`node scripts/cloudflare-zone.mjs` prints these settings and `node scripts/cloudflare-zone.mjs --apply` sets them, using the credentials in `.env.cloudflare.local` (the Global API Key, or a token with Bot Management, Cache Settings and Zone Settings edit permissions). The same switches are in the dashboard under Security → Bots, Caching → Tiered Cache and Speed → Optimization.

## Live airport services

- `worker.ts` wraps the existing Vinext handler and exposes `/api/live`, `/api/config`, `/api/contact` and `/api/metrics`.
- The Cloudflare target enables `global_fetch_strictly_public`, so scheduled checks of `henokabraham.com` reach the public Worker instead of bypassing it for the zone origin. The deployment guard and scheduled check verify this flag.
- The scheduled handler checks the allowlisted public GitHub repositories and deployed projects every 15 minutes. KV namespace `henokabraham-com-live-data` (`8936ecfe40dd4fc9a30d109c058baf60`) stores public snapshots only. Project checks older than 45 minutes are labelled as previous checks. I removed the KSFO METAR feed and its "Live at SFO" panel on 2026-09-13; the stored `weather:v1` key expires on its own within a day. A blocked automated website check is reported as unverified, not an outage.
- Turnstile widget `0x4AAAAAAEulQFo5cYQJ5FfZ` covers the apex and Workers address. `TURNSTILE_SECRET_KEY` is a Cloudflare Worker secret, never a public Vite variable. Verification checks the hostname and `contact` action. The server limits bodies, validates fields and requires same-origin POSTs. Contact rate limit: three attempts per minute per IP at each Cloudflare location.
- `CONTACT_EMAIL` can send only to the verified owner inbox. Messages use `tower@henokabraham.com` as From and the validated visitor address as Reply-To. Email Routing was enabled after confirming the zone had no existing MX/SPF records. Contact text is not saved in KV or analytics. Delivery failures return an error; the UI never claims success on failure.
- Web Analytics site `fd6c9f62f2cb41a382b93bf66fc4cf0c` is installed by the application, with automatic injection disabled. Custom measurements are atomically aggregated in D1 database `henokabraham-flight-stats` (`1ce8af41-87e5-4f67-b06f-76fc50cce697`), binding `FLIGHT_STATS`, table `flight_metrics`. The schema is in `migrations/0001_flight_metrics.sql`. Summaries older than 90 days are pruned hourly. Both are skipped for Do Not Track or Global Privacy Control. The footer explains what is measured.
- Custom data is grouped by UTC hour, event, device class and motion preference. Each row holds count, sum, minimum and maximum; individual events are not stored. Events: `scene_ready_ms` (navigation to scene ready), `scene_fps` (the first visible two-second frame window after warm-up), `scene_unavailable`, `scene_asset_failure` (up to three reports per visit), `scene_scroll_fps`, `scene_scroll_p95_ms`, `scene_scroll_jank_pct` (one moving-frame window per early/middle/late tour region, at most three per event per visit), and `project_open` (up to ten selections per visit). These are capped samples, not totals of all errors or clicks. No visitor IDs, IPs, URLs, message text or raw user agents are recorded in these custom events.
- The metrics endpoint accepts only the documented events and bounded numeric values, with 30 requests/minute/IP at each location. Worker logs sample 10% of invocations and do not log contact payloads.

Inspect Web Analytics and Workers logs in the Cloudflare dashboard. Open the D1 console for `henokabraham-flight-stats` to inspect custom summaries, for example:

```sql
SELECT event, device, SUM(samples) AS samples,
  SUM(total) / SUM(samples) AS average_value,
  MIN(minimum) AS minimum, MAX(maximum) AS maximum
FROM flight_metrics
WHERE hour >= strftime('%Y-%m-%dT%H:00:00Z', 'now', '-1 day')
GROUP BY event, device;
```

### Verification

```sh
node --experimental-strip-types scripts/check-edge.mjs
node --experimental-strip-types scripts/check-metric-storage.mjs
node scripts/check-bay-rendering.mjs
node scripts/check-bay-scenery.mjs
npm run check:cloudflare
node scripts/check-scheduled.mjs
```

The scheduled check runs the built Worker directly in local workerd and writes only local KV. This avoids the installed Miniflare static-assets router, whose generic scheduled trigger targets the asset router instead of the application. It requires network access for the public data feeds. `scripts/refresh-live-snapshot.mjs` produces a public-data-only KV bulk payload in the system temporary directory for initial seeding; normal refreshes use the Worker cron.

New bindings are configured only for `DEPLOY_TARGET=cloudflare`. The original Sites build stays available; live services gracefully report unavailable there unless equivalent bindings are configured.

Analytics Engine activation was attempted in the dashboard, but the upload API continued rejecting the entitlement (10089). The final Worker uses D1 for its custom measurements, so it does not depend on that service. Web Analytics remains enabled separately.

To initialize the same schema in a newly provisioned metrics database, first update the dedicated database ID in `vite.config.ts` and the deployment guard, build, then apply the idempotent schema:

```sh
npx wrangler d1 execute FLIGHT_STATS --remote --config dist/server/wrangler.json --file migrations/0001_flight_metrics.sql
```

The production schema was applied before the first D1-backed deployment. Do not run database initialization against an unrelated database.

## Review fixes deployed

Source commit `3bbe137` fixes whole-image/shade bitmap orientation, gives chapter navigation an opaque high-contrast surface with visible focus and active states, restores the sampler-budget check, and makes self-monitoring use public routing. Desktop and 390 × 844 browser checks, TypeScript, rendering/scenery/flight checks, edge/metric checks, the deployment dry run, and the built scheduled handler passed. The public-routing flag was read back from the deployed Worker; a fresh public project check was published to KV to clear the older error immediately. No database schema or runtime secret changed.

## Adaptive flight performance deployed

Source commit `742f298` adds sustained frame-time quality adjustment, compact desktop scenery, half-resolution AO, bounded lighting refreshes, and faster scroll following. Desktop terrain/building triangles fall 76.5%; the tested full-route easing settles within 1.6 seconds at 30/60/120 Hz. TypeScript, lint, performance/rendering/lifecycle/scenery/flight checks, edge/metric checks, and the Cloudflare dry run passed. Local 1280 × 720 and 390 × 844 browser checks rendered without console warnings or errors, with roughly 120 FPS in sampled settled opening/climb/Bay windows on the test computer. Phone-sized viewport checks are not physical-phone benchmarks or a controlled before/after FPS comparison. The live homepage and APIs returned 200; both changed application/rendering chunks matched local SHA-256 hashes after deployment. New moving-frame telemetry uses the existing D1 schema.

## Drifting blue-sky opening deployed

Source commit `8ae9181` keeps the existing blue sky visible after the scene is ready, moves the photograph and two cloud layers gently, and reveals the flight over 180 px after the first 48 px of scrolling within the hero. The 3D animation and lazy scenery wait while covered. Desktop and 390 × 844 browser checks confirmed the sky at rest, moving cloud positions, the aircraft after scrolling, and the restored sky on return. TypeScript, performance/rendering/lifecycle checks and the Cloudflare build passed. Reduced-motion visitors keep a static sky.

The cloud-motion enhancement (`e5cdd4d`) raises the moving layers into view, increases their contrast, and uses continuous 26/42-second drift loops with fading resets. The sky photo moves over 32 seconds. Desktop and 390 × 844 visual checks and the production build passed; the scroll reveal and motion/visibility safeguards are unchanged.

## Airborne Dreamliner showcase deployed

Source `e79ed3d` replaces the ground departure with a moving-sky fly-in and engine, wing and tail close-ups. The credited FlightGear exterior has 4K fuselage/engine textures and 105,622 triangles merged into ten main material groups; it uses HDR reflections and a personal livery. The original terrain experiment remains in the repository and is absent from active scene imports. Source `1151a4d` changes the HTTP Early Hint from the old tile manifest to the universal sky image. The aircraft remains a low-priority, motion-qualified HTML preload.

The opening stays free of WebGL draws until scrolling, with a bounded wake-up that handles parent/renderer RAF ordering. WebGL, audio and clouds suspend offscreen/hidden; reduced motion keeps a static sky. New regression tests cover late fetch/parse completion and single disposal after unmount. A shader warm-up cleanup race found during development was fixed; final visual checks recorded no new console errors. Desktop spot checks reached about 120 fps on the test computer; phone-size checks verify composition, not physical-phone performance.

## Header integrated into the sky

Source `dc98073` positions the transparent header over the existing moving sky, removing the white band and border. Darker navigation text keeps the links readable. The flight label and secondary project shortcut appear after the header has scrolled away; opening text keeps clearance below the header on desktop and mobile. The reduced-motion and unavailable-scene fallbacks retain the same header clearance without revealing duplicate controls.

## Google Sans typography

Source `d4170cb` replaces the previous UI fonts with Google Sans from my own download. Normal and italic WOFF2 subsets retain the weight (400–700), optical-size and grade axes. The normal font is 133,964 bytes and preloaded; the 142,504-byte italic face loads when used. Both use content-hashed URLs and one-year immutable caching. Text uses `font-display: swap` with a system fallback. The font license and reproducible preparation script are included; no third-party font request is required.

## Portfolio stops and personal flight atlas

Source `daa2422` shortens the native scroll journey from 680svh to 420svh and reveals the first project at 18% progress. The stops preview Downshift, the iPhone–Wear OS bridge, the flight log and the rest of the portfolio. Project buttons open the existing briefings without leaving the tour. Opening a briefing pauses rendering/clouds and closing it restores focus without scrolling. Project descriptions, about text, section introductions and repeated camera captions were shortened.

The new flight atlas uses a local 49 KB Natural Earth map with selectable great-circle routes, year filtering, estimated distance totals and boarding-pass details. Date-line crossings are split, repeated trips count correctly and large totals use compact labels. The user's actual flight records are still pending: `app/personal-flights.ts` is empty and the live view says “Routes coming soon,” with unknown totals. No sample trips or the earlier site-reference notes are presented as personal history. See README for the public data shape.

## Flighty atlas populated

Source `a485c82` imports 370 historical flights from my own exported data: 42 airports, 13 countries/regions and 325,585 estimated airport-to-airport miles. Three cancellations are excluded. Four diversions retain their scheduled destinations and map their actual arrivals; a return-to-origin record counts as a flight without estimated route miles. The list uses 20-record pages while the map and totals retain the full year selection.

Only allowlisted public flight fields are included. The private CSV, booking references, seats, notes, tail numbers and exact travel times remain outside the published assets and repository. The source was pushed to the verified private GitHub repository.

## Summary-only passport view

Sources `889c501`, `7fcd817` and `5bd9720` remove the individual flight list and boarding passes. Import records now live under `scripts/data`, outside the application dependency graph. Every build generates only aggregate totals, unique routes, airports and country order for all time and each year. Exact travel dates, flight numbers, airlines and aircraft details are absent from runtime data.

The map labels international airports with local flags. Thirteen circular flags overlap in a single responsive row below the totals; selecting a country highlights its routes. Flags follow the first logged visit within the selected year or the complete log. All-time order: United States, Canada, United Kingdom, Ireland, Mexico, Colombia, Denmark, Netherlands, Argentina, Japan, Switzerland, French Polynesia and South Korea. Source credits and the MIT license for the flag assets are in `public/credits/country-flags.txt`.

## Airline logos added

Source `8a0dae1` adds a compact airline-logo strip below the country flags. The 14 airlines are sorted by aggregate flight count within the selected year or complete log. Selecting a logo shows the airline name and total; changing the year clears the selection. The public summary adds only carrier names, local logo paths and counts, with individual flights still excluded.

Ten SVG logos are sourced from Soaring Symbols and four PNG logos from Kiwi’s airline image collection. All are served locally; source URLs, the collection license and the Wingo/Aero Republica naming source are recorded in `public/credits/airline-logos.txt`. No runtime logo service is required.

## Transparent airline marks and brand colors

Sources `54faff3` and `035e597` remove the white tiles and replace American, Frontier and Spirit’s boxed PNGs with transparent SVGs. The final layout uses spaced marks and wraps on narrow screens; the country flags continue to overlap. American uses blue, silver and red, Spirit yellow, and United blue. Other airlines retain their representative colors, with dark blue ink brightened for the navy surface. Hover, keyboard focus, selection and year filtering are retained. Updated asset provenance is in `public/credits/airline-logos.txt`.

## Review fixes: lighter aircraft, policy headers and a sharing card

Sources `7dea1fa` through `6311227` act on a full review of the live site. Per-visit transfer, measured headless on desktop and phone, falls from 8,067 KB to 3,240 KB: the aircraft is Draco-compressed from 5,272,964 to 1,308,580 bytes (16-bit positions so the livery and wing-flex shaders keep their metre coordinates), the lighting map is halved to 512 × 256 and 380,181 bytes, and the 245 KB decoder is served under the same content version. The model and lighting preloads leave the document; the scene fetches them once it decides to run, and reduced-motion visitors and browsers reporting `saveData` or a 2G-class link get the static sky at about 1.5 MB with no model request. The lighting map no longer delays the aircraft.

The retired terrain experiment's tiles, imagery and scenery moved from `public/` to `archive/`, so `dist/client` drops from 164 MB in 3,489 files to 20 MB in 92, and its plain paths answer 404 rather than being uploaded on every deploy. The document response (GET and HEAD) carries HSTS, `nosniff`, `X-Frame-Options: DENY`, a referrer policy, a permissions policy and a CSP limited to `frame-ancestors`, `base-uri` and `form-action`; static files carry `nosniff` from `_headers`. Links to the site now show a 1200 × 630 card captured from the tour, with a canonical URL, a site name, a sitemap, a robots file, a real `favicon.ico` and a 180 px `apple-touch-icon.png` for Messages and Safari; the page and card were confirmed as served to the `facebookexternalhit` agent Messages uses, with no challenge markup.

Scene defects fixed: the sound button no longer enables under reduced motion, where no render loop could ever drive it; a lost WebGL context closes the AudioContext and resets the button instead of stranding it ON and disabled; the scroll handler lays out once per frame; the Devices stop slides the frame right on wide screens so the headline sits on sky rather than across the fuselage titles, and the Apps and Devices headlines carry a soft halo. The atlas caption names every airport for screen readers, departures rows have separated accessible names, the return-to-origin record no longer emits an empty path, route coordinates round to a tenth of a map unit (the document shrinks by 16 KB), and Turnstile is removed before the form unmounts after a send. `npx oxlint app lib scripts` is clean.

Verification: type check, lint and all thirteen offline checks pass, the tour check now decoding the Draco mesh with the reference codec. Live GET and HEAD headers, versioned asset caching, the 404s for retired paths, the `?chapter=bay` deep link and the reduced-motion, data-saver and phone sessions were confirmed against production; the site's own console is clean, the only messages coming from Cloudflare's Turnstile frame. The owner then ran `scripts/cloudflare-zone.mjs --apply`: Bot Fight Mode and JavaScript Detections are off, Smart Tiered Cache and Early Hints on. The challenge script is no longer injected into the document, the edge answers the document with a 103 carrying the font and sky preloads before the 200, and the sampled static assets are edge hits.

## Seven suggestions deployed: AVIF sky, phone aircraft, lazy three.js, 404 page, starter-kit prune, head tags, CI

Source `929a39f` serves the opening sky and cloud sprite as AVIF through `image-set()` (356,881 to 57,497 bytes and 556,541 to 40,267), keeps the JPEG and PNG as the plain background for browsers without AVIF, and preloads the AVIF with its type in the Early Hint. Viewports under 800 px fetch `dreamliner-787-9-phone.glb`, the desktop Draco buffers under 2048² textures (611,036 bytes; textures 888,538 to 190,988), built by `scripts/prepare-dreamliner-phone.py` and versioned with the other scene files (`824946c4830aca4a`). The scene imports its nineteen three.js classes through `lib/dreamliner-three.ts` and is mounted lazily from `app/scroll-departure.tsx`, so the page chunk no longer imports three.js at all: the library chunk is 137,581 bytes on the wire (was 182,174), the page chunk 63,961 (was 71,748), and a reduced-motion visit fetches neither the library nor the scene.

`ec1ce55` adds `app/not-found.tsx`, the tour's sky behind "No such gate." with links to the open sky, the projects and the flight log; the response stays 404. `13df807` removes 58 unused starter components, the sidebar hook and eight dependencies, taking the stylesheet from 40,732 to 20,117 bytes on the wire; `oxlint` passes for the whole repository with `public/draco/` excluded. `7544e48` adds `theme-color` (#6398cf, sampled under the header) and a Person JSON-LD block. `555464d` adds `.github/workflows/checks.yml`: types, lint, the Cloudflare build and every check script on each push.

Measured on the live site after deploy (Worker version `0210ddfe-9530-451b-99dd-2323a8aafe7e`) with headless Chromium, bytes on the wire per full visit: desktop 3,240 KB to 2,307 KB, iPhone 3,240 KB to 1,570 KB, reduced motion 1,520 KB to 382 KB. The phone model, decoder, lighting map and both AVIF files serve with immutable or edge-cached headers; the 200 response carried the AVIF hint at once, and the edge's cached 103, which kept the earlier JPEG hint for a few minutes after the deploy, turned over to the AVIF on its own. Phone close-ups of the engine and tail were compared against the desktop model at the same viewport and are indistinguishable. The first CI run failed in `check-scheduled.mjs`, which had fetched the real weather and GitHub feeds; `e7dc67e` and `127988f` give it canned upstreams through Miniflare's `fetchMock` (undici pinned to Miniflare's own 7.24.8, since it accepts only that version's MockAgent) with `--live` for a manual end-to-end run, and the workflow passes on both branches.

## Golden Gate above the fog, and edge to edge on phones

Source `c0f1dd6` puts the Golden Gate's towers and cables on the horizon of the sky photograph, from my own bridge model (`archive/models/golden-gate-bridge.usdz`, 3.75 M triangles; the file itself was later dropped from the repository since its source and licence are unknown to me): `scripts/prepare-golden-gate.swift` exports world-space buffers with Model I/O, `scripts/render-golden-gate.mjs` renders the 522 meshes that rise above the fog in three.js (International Orange, the sky's warm upper-right light, a three-quarter aerial camera), and `scripts/make-golden-gate-sprite.py` fades the fog in, tints toward the horizon haze and writes `public/images/golden-gate.avif` (14,362 bytes, PNG fallback 67,168). `.bay-landmark` sits inside the sky poster, anchored to the photograph in image fractions through container units, so it lands on the haze band at every aspect ratio, drifts with the sky and passes behind the aircraft; portrait centres it where the cover crop keeps the photo's middle. The 404 page carries it too. I then wanted it lower left: the anchor moved from the horizon (x 0.57, fog line y 0.60, 0.25 of the photo wide) into the cloud deck at x 0.33, y 0.72, 0.30 wide, with less haze for the nearer distance (Worker version `b79f3ff5-9e90-4672-9477-61b4099f9e14`); portrait sits it at x 0.47, y 0.66, 0.24 wide, above the chapter bar with both towers in frame.

`b036710` makes phones edge to edge: `viewport-fit=cover` with every anchored control inside the safe areas, the journey's sticky frame sized to the large viewport so the sky and the aircraft fill the screen once Safari's bars collapse while the chapter bar stays anchored to the small viewport, no overscroll bounce, sky behind any overscroll, no text inflation in landscape, and the short-landscape layout now clears the chapter bar (the preview card had overlapped it by 10 px). `site.webmanifest` with 192/512/maskable icons and the Apple web-app tags let Add to Home Screen open the site full screen behind a translucent status bar. Verified live in WebKit and Chromium phone emulation (portrait, landscape, the 404 page) with a clean console and the checks workflow green; not yet on a physical iPhone, where Safari's collapsing bars and the Home Screen mode are the parts to watch.

## Golden Gate turned to run up the left edge; Immersive control

Source `d1afd09` re-renders the bridge looking down its span from beyond the north tower (330 m up, a little to the ocean side), so the near tower stands large at the lower left and the span recedes toward the horizon; a second render pass records world height and camera distance per pixel, from which the sprite step fades the fog and thickens the haze from 20% at the near tower to 55% at the far one (`public/images/golden-gate.avif` 28,900 bytes, 373×892). The landmark is sized by height, a third of the photo with its bottom at y 0.9 and x 0.17; portrait uses x 0.4, y 0.7 and 0.22. `3ade26a` adds an Immersive control to the journey's bottom row: a browser never hides its own bars for a page, so the button asks for fullscreen through the Fullscreen API with the webkit names iPhone Safari has used since 16.4, hidden where fullscreen is unavailable and in a Home Screen launch, an icon on narrow screens. Worker version `66eac704-e3af-432e-b34d-57caac8a5614`. Verified live: landmark at x 30–171, y 525–864 on 1440×900 and x 24–93, y 317–482 on 393×659, the control entering and leaving fullscreen in Chromium, console clean. Playwright's WebKit reports fullscreen unavailable, so the button's iPhone behaviour rests on WebKit's documented support, not on a device run.

## Portrait bridge seated on the cloud deck; iPhone Immersive card; Safari 26 bar colour

My iPhone screenshot showed the aerial sprite stacking its towers at the left edge of the portrait crop, the near cables cut off and the far tower floating above the cloud horizon, and no Immersive control in the bottom row. Source `2554e98` adds a second sprite for portrait, `public/images/golden-gate-portrait.avif` (36,644 bytes, PNG 191,562, 518×669), rendered with the camera 200 m up just above the tower tops (`render-golden-gate.mjs --camera=1250,200,-130 --target=0,100,-40 --fov=20 --size=2400x2400`; the script now takes camera, target, field of view and size options and writes its horizon and tower rows, and the sprite script takes `--out` and `--dir`). The stylesheet pins the render's eye level, 7.4% of the sprite's height above its crop, to the photograph's horizon at y 0.56 (`--landmark-anchor`, `--landmark-ratio`), x 0.42, a fifth of the photo tall, so both towers stand on the cloud deck and the span climbs up and to the right from the lower left, clear of the chapter bar. Desktop keeps the aerial sprite unchanged.

The missing control was WebKit: the Fullscreen API on an iPhone covers only video, bug 206854 is still open against Safari 27, and the page had correctly hidden a button that could do nothing. On an iPhone the control now opens a card (the site's Base UI dialog) explaining the one route that fills the screen, Share then Add to Home Screen, and is still hidden once launched from the Home Screen. Research for this pass, from WebKit's tracker and the Safari 26 write-ups by Jahir Fiquitiva and Pavel Larionov: Safari 26 ignores `theme-color` and tints its Liquid Glass bars from the `background-color` of the fixed or sticky element within a few pixels of each screen edge, falling back to its own grey; the journey's sticky frame, which sits at both edges on the opening, now carries the sky colour under the poster. Worker version `831e01f8-60af-457f-b332-3245cfa05656`. Verified live in Chromium and WebKit phone emulation: landmark at x 14–140, y 420–583 on 393×713 (x 25–141, y 389–539 on 393×659, the same on the 404 page), the card opening and closing with the fullscreen API disabled, no control with `navigator.standalone`, the desktop button unchanged, all thirteen checks and the workflow green. The bar colour and the Home Screen launch remain to be seen on the physical iPhone.

## Live at SFO panel removed

I wanted the "Live at SFO" airport-conditions band gone. Source `9cc61cc` removes the panel from the page and everything that existed only for it: `LiveAtSfo` and the Fahrenheit formatter in `app/airport-services.tsx`, its styles, the KSFO METAR fetch and parser in `server/live.ts`, the `weather` field of `/api/live` (now `{ projects }`), and the canned METAR upstream in `scripts/check-edge.mjs` and `scripts/check-scheduled.mjs`. The scheduled handler keeps the GitHub repository and website reachability checks that feed the latest-check lines inside the project briefings, still every 15 minutes into the same KV namespace; the stored `weather:v1` key expires on its own within a day. Worker version `b9484484-2705-414c-b5d0-41335a79904e`. Verified live on desktop and phone: no panel or text, the departures section directly after the journey, `/api/live` returning only the project snapshot, and a briefing showing "LATEST CHECK · Sep 13, 2026 · 07:00 UTC"; all thirteen checks pass, the scheduled check writing five project records.

## Downshift's screens on a real device render

The first pass at showing Downshift on an iPhone used a CSS-only frame; it didn't read as realistic. Source `54f752c` replaces it: the scroll teaser now shows three of Downshift's screens (dashboard, performance, settings), each composited onto its own phone in a three-phone fan from a free MockupNest PSD, plain image now, no card, no button, not clickable. `scripts/make-downshift-mockup.py` recovers each phone's rotation from its screen mask (OpenCV `minAreaRect`) since the smart objects are placed as plain unrotated rectangles internally and the fan is baked into each mask, not into Photoshop's placement transform. That PSD's free tier is personal-use only and doesn't permit redistributing the mockup file, so the composited `public/images/downshift-mockup.avif`/`.png` are gitignored and deployed straight from this local build rather than committed, the same way the Golden Gate bridge asset was handled earlier. The project briefing's image goes back to the plain screenshot it used before that CSS pass, so it keeps using the one committable asset. Worker version `f2faa1e6-389c-459d-beee-5839c7da3849`.

A first render had the screen content rotated the wrong way relative to each phone's own tilt (`Image.rotate`'s sign convention is opposite `minAreaRect`'s), which read as the app floating unglued from the glass rather than sitting on it; caught by measuring the status-bar text's angle against the bezel's in the rendered pixels, not by eye, and fixed by negating the angle before rotating. Verified: TypeScript, lint, a full `build:cloudflare` and every check script pass with the gitignored image entirely absent (mimicking a fresh clone) and present (confirming Vinext copies `public/` regardless of git state); the live PNG and AVIF byte sizes match the local build exactly. Not yet re-confirmed by eye on the live page or on a real phone/landscape viewport — this session's Chrome automation stopped responding to screenshot requests on this WebGL-heavy route partway through and didn't recover.

## The front phone plays a real recording

A still screenshot wasn't enough — I wanted the front phone actually running. Source `51073ba` adds `--video-slot` to `scripts/make-downshift-mockup.py`: it leaves that phone's screen transparent in the background composite and exports its flat (unrotated) screen shape plus placement percentages, and `app/scroll-departure.tsx` layers a muted, looping `<video>` into that spot with a `mask-image` and a matching `rotate()`. The recording is my own footage — RevMatchApp's built-in demo sweep (temporarily extended from 3s to 12s, and the "not connected" prompt card temporarily hidden, both reverted with `git checkout` right after) played in the iOS Simulator, captured with `xcrun simctl io recordVideo`, trimmed and crossfaded into a seamless ~8.5s loop with ffmpeg. Unlike the PSD-derived background and mask, the video isn't licence-restricted, so `public/video/downshift-dashboard-loop.mp4` is committed normally. Reduced-motion visitors still get the older all-static three-screen image.

I verified the placement geometry by swapping the video for a static crop of the same screenshot in the live DOM and confirming it filled the screen cutout with no gap or overlap against the frame; I could not get the actual `<video>` to play in this session (`document.hidden` was `true` for every tab this session opened — no attached display — so the browser withheld the decode regardless of page complexity, confirmed by the same stall on a trivial static-file page with no app code at all), but the file plays correctly when a tab is navigated to it directly, and every other check (TypeScript, lint, build, full check suite with the gitignored assets absent and present, live byte-for-byte match on all three new assets) passed. Worker version `f8bedc8f-4395-4047-897a-e7602c5a2204`. Autoplay-in-context on a real visible tab is the one thing left to confirm by eye.

Henok confirmed the video does autoplay live (the environment limitation above was real, but only stopped me from checking it myself) and reported the front phone's corners looked square instead of rounded, notch included. Source `971f9a8`: CSS `mask-image` defaults to _alpha_ mode for a raster source — luminance is only the default for an SVG `<mask>` resource — and the mask PNG was a plain grayscale image with no alpha channel at all, so it was fully opaque everywhere and clipped nothing; the video's bare rectangle showed straight past the frame's rounded corners. Fixed by moving the shape into the alpha channel and setting `mask-mode: alpha` explicitly rather than relying on the default. Worker version `2545e4e1-f048-4046-b9bd-2e413058e2a3`; live mask PNG byte size confirmed matching the local build.

## The mobile chapter bar's pills, sized to their own labels

Henok's own phone screenshot showed the bottom chapter bar's "Apps" pill as a big, unbalanced block next to plain-text neighbours. Source `b846843`: the mobile row was still the equal-width `flex: 1` layout built for its old digit-only labels, and each label span still carried the desktop rule's fixed 76px width — full words in a forced-equal, forced-narrow box, so the short one ballooned and the long one ("Flight log") was squeezed tighter than its own text. Buttons now size to their own content (`flex: 0 1 auto`, label `width: auto`) with a small font bump (0.62rem to 0.68rem) since space isn't wasted on equal segments anymore. Verified: TypeScript, lint, build and all checks pass; I don't have a working narrow-viewport screenshot tool this session (`resize_window` doesn't affect the actual render here), so I previewed it by injecting the equivalent computed styles into the live 855px-wide tab, forcing the chapter bar to a fixed 335px and then 308px box (iPhone-14-class and iPhone-SE-class content width after the bar's 7% side margins) — both fit all five labels with even spacing and no overflow. Live CSS confirmed serving the new rule (`.68rem` present in the deployed stylesheet). Not yet seen on an actual phone.

## The Apps/Devices sky no longer sits behind a dark panel

Henok's own phone screenshot of the Devices stop showed a dark, flattened band across the top third of the frame — the flight label, "Skip to projects" and the headline all sitting on what read as a solid dark panel rather than the photographed sky. He asked for the panel gone but the text kept readable. Source `3ef888a`: at that stop the header text is plain dark ink (`#163448`, no text-shadow at all on the flight label or skip link) sitting on a `linear-gradient(180deg, #a7c7ebf2 0%, #b9d7efee 30%, #d6e8f373 35%, transparent 41%)` painted over the top ~41% of the frame — a flat, fairly opaque wash meant to guarantee contrast for dark text regardless of what the fuselage and sky render underneath, which is what read as a dimming "panel" over the photo. I removed that gradient and gave the flight label, skip link and story block (eyebrow and description inherit it) the same soft white halo `text-shadow` the headline already carries at these two stops — contrast now comes from a glow around each glyph rather than a rectangle painted over the scene.

I couldn't confirm this by eye against the real dark fuselage: this session's tabs render with `document.hidden` true (no attached display), which blocks the WebGL tour canvas the same way it blocked the video autoplay checks earlier in this document, so every render I could produce showed only the static sky poster, never the aircraft passing overhead. What I could and did confirm live: the text really was flat dark ink with `text-shadow: none` before this change (read via `getComputedStyle` after forcing `data-phase="liftoff"` on the section), and the new halo applies with the intended values after it, reusing a technique already proven live on the headline rather than inventing an untested one. TypeScript, lint, build and all checks pass. Worker version `642e6165-2547-4444-a884-449fe37af54a`; the live stylesheet at the new build hash carries the halo color and no longer contains the removed gradient's `a7c7eb` stop. Not yet seen against the actual aircraft on a phone.

## The halo everywhere, not just Apps/Devices

Henok sent a second phone screenshot, this time of "Open sky" (the very first, static-sky stop) — the header and hero copy read washed out and pale there too, while the sky photo underneath stayed sharp. On desktop the same stop renders with solid, fully-opaque dark ink, matching what he'd already called solid elsewhere; I couldn't reproduce a mobile-only cause in the CSS, since `.bay-flight-label`, the story block and `.terminal-header` (the persistent site header, a sibling of the journey, always floating transparent over the same sky photo) carry the same ink colour and no font-size- or width-gated opacity anywhere in either stylesheet. Rather than guess at a browser-specific cause I can't reproduce in this environment (no attached display, so no WebGL and no real phone to hold), I asked whether the washed-out look was a passing frame or the resting state; Henok said it's a mobile issue.

Source `4559849` widens the halo added for Apps/Devices in the previous entry to every stop of the journey on mobile (dropped the `[data-phase='roll']`/`[data-phase='liftoff']` qualifiers — the more specific headline rule for those two stops still wins where it applies) and added the same halo to `.terminal-header`, which sits outside the journey entirely and previously had no shadow at all. This treats the symptom Henok can actually see rather than a specific diagnosed cause, since I have no way to reproduce mobile-only rendering behaviour in this session. Verified: the computed `text-shadow` on the header, eyebrow, headline and flight label all carry the new halo in a live dev-server tab (checked via `getComputedStyle`, injected mobile-width CSS since `resize_window` still doesn't affect the real render here); TypeScript, lint, build and all checks pass. Worker version `bfd94d93-9a1c-4c5a-ad34-0b5604a8d302`; the new CSS build hash serves the halo on both rules, confirmed by fetching it directly (the HTML document briefly kept referencing the prior deploy's hash at the edge, same lag as the last two deploys — the asset itself was already live). Whether this actually fixes what Henok is seeing, rather than just adding a halo that was already unnecessary, is the one thing I can't verify without his phone.

## Garage: an interactive GT350R while Henok slept

Henok asked for a section on his car, dropped a Sketchfab USDZ download of a 2016 Shelby GT350R at `~/Downloads`, said it's gray with blue stripes, then went to bed with "do as much as you can now to show me in the morning." Two decisions before touching anything: the model's own metadata named a real licence, Ddiaz Design's Sketchfab download, CC BY-NC-SA 4.0 — non-commercial, share-alike, attribution — which matters more here than it did for the Golden Gate bridge model (unknown licence, so kept local-only and only a flat rendered sprite shipped) because an interactive viewer ships the actual geometry and textures to every visitor's browser, not a picture derived from them; I surfaced that before writing any code and Henok chose to proceed with credit rather than find a different model. Second, he picked a live interactive viewer over a static render, which meant a second real-time WebGL context on a page whose hero already has its own render budget — worth gating carefully, not worth skipping given the choice.

The conversion (550 mesh prims in the source USDZ, merge by material, recolour the body, fix trim that rendered wrong) was substantial enough asset-pipeline work that I ran it in a background agent rather than my own context, the same call this session already made for the mockup video capture. Two rounds: the first pass merged everything and got the paint recoloured from the source's default scheme to a placeholder gray/blue; Henok sent two real photos of his actual car mid-flight, so I sampled clean patches directly from them (`#5f656b` body, `#092e70` stripe — both notably darker/more saturated than the first guess) and sent the corrected values back rather than wait for morning. The second round, since I'd told the agent to keep going rather than block on my sign-off overnight: the front splitter, rocker skirts, rear wing and exhaust surround were rendering plain white, traced (not guessed) by sampling every pixel a second small trim texture is actually UV-sampled at — only three colours are ever used across the whole atlas, and two of them were the bug. The exhaust tips shared that same texture cell, so recolouring it would have made them black too (wrong, real tips are metallic); split into their own primitive by position clustering instead, verified against a silhouette render. I'd separately flagged the mirror caps rendering stripe-blue instead of black — a real bug, not a placeholder colour: the mirror geometry is part of the body-paint mesh and its UV unwrap happens to land inside the stripe's V-band by coincidence. Not spatially separable (no 3D gap between mirror and fender), so isolated by combining outboard position with stripe-band UV instead, verified bimodal with nothing in between. Two faint bright slivers I'd also flagged turned out, on raycasting the exact pixels and reading back the real triangle normal and texture value, to be specular highlights from the verification viewer's own lights hitting correctly-modelled body creases — not a data bug, left alone.

Once the model looked right, I moved the conversion pipeline into the actual repo: `scripts/prepare-garage-car.py` (plus a `garage_usda_parser.py` module) takes the raw USDZ path as an argument, extracts and runs `usdcat` itself, and does everything the background agent's scratch scripts did. I re-ran it from scratch against the real USDZ file before trusting it — the output GLB came out byte-for-byte identical to the one already sitting in `public/models/`, which is a stronger verification than reading the code and hoping. `scripts/check-garage.mjs` (new) checks the GLB is under budget, Draco-compressed, carries its licence in both `asset.copyright` and the top-level `extras` object (matching the aircraft GLB's own convention, not `asset.extras` — I got that nesting wrong on the first pass of the check script and caught it by actually reading the shipped file's JSON rather than assuming), and that the credits page and viewer component still carry their required content. `GarageScene`/`GarageSection` (new) mirror `dreamliner-scene.tsx`'s lifecycle contract — disposal sets, `webglcontextlost`, reduced-motion, `document.hidden` suspension — but render on demand (OrbitControls' `change` event plus a short settle window for damping to finish, rather than a permanent RAF loop) and don't even open a WebGL context until the section is within 300px of the viewport, since it sits below an already-heavy hero. Worker version `e01fbf7e-7ea5-46ea-bb3a-171f3c98865a`.

I could not see any of this render for most of the session, same `document.hidden` limitation as the mockup video: every tab here reports hidden, so the canvas never paints. This time I found a way around it for verification purposes — `Object.defineProperty(document, 'hidden', {value: false})` plus a `visibilitychange` dispatch, which the viewer's own listener picks up — and got a real frame on the live site. It caught something real: the body read lighter/more silver than the intended `#5f656b` under the lights and clearcoat I'd picked blind. Cut the light intensities and tone-mapping exposure, and pulled the clearcoat and roughness back toward a more matte finish (specular reflection of the dark studio backdrop was adding more variance — dark in shadow, bright in highlights — than the flat colour could stand up to). Source `69e83bb`, Worker version `60a54aa9-3ad4-40ad-a4e6-fe823fa4328b`. A raw pixel readback off the canvas came back all zeros (the renderer doesn't set `preserveDrawingBuffer`, so `readPixels` after presentation gets nothing), so the numeric check that exists — sampling a screenshot crop, door panel came back close to the target gray — is weaker evidence than I'd like; the exposure and material balance are worth Henok's own eyes before I'd call this finished, the way the mockup's autoplay needed his phone to actually confirm.

TypeScript, lint, `build:cloudflare`, and all fourteen check scripts (`check-garage.mjs` included) pass. The live GLB is confirmed byte-identical to the local build; the credits page is confirmed live (`/credits/garage.html`, a 307 to the extension-less path, same pattern the aircraft credits page already uses). Not yet touched: no pruning of the interior or engine bay (54% of the triangle budget between them, on an exterior-focused viewer), and the pre-existing scroll-jacked hero fights programmatic and anchor-link scrolling into the sections after it, including the new Garage nav link — noticed while trying to verify this, not something this session introduced or fixed, since it's a bug in navigation shared by every section after the hero, not specific to the new one.

## The roof, fixed properly, and a background that isn't a void

Henok woke up, looked at the Garage section, and asked for two things: fix the roof (his own car's roof is gray with blue stripes like the body), and give the viewer much better lighting and background — the flat dark void behind the car wasn't good enough.

Rather than keep fighting this session's two known verification obstacles (`document.hidden` blocking WebGL, and the scroll-jacked hero making the live site slow to reach), I built a throwaway harness outside the site entirely: a static HTML page loading the exact same three.js/GLTFLoader/DRACOLoader/HDRLoader code path as `garage-scene.tsx`, served over a plain Python HTTP server, driven by Playwright's own bundled Chromium (headed, not headless — the sandboxed SwiftShader software renderer this environment falls back to under headless Chromium can't create a WebGL context at all here, confirmed by trying; a headed launch uses the real Metal-backed ANGLE path and works). This let me iterate on lighting and geometry with real renders and real raycasting in seconds, independent of the live site's own quirks — a better tool for this kind of visual debugging than the live-site patching used previously, worth reaching for again.

The roof bug wasn't a recolor problem. Sampling the actual `PaintA` swatch at the roof panel's own UV coordinates (not just at a screen pixel) showed only the two colors the recolor step already handles correctly — the classification was working. The real cause: the source artist's UV layout for this one panel maps almost the entire roof into the "stripe" swatch cell, rather than tracking the body's gray-with-stripes pattern the way the hood and trunk do. Bucketing both the hood's and the roof's actual sampled colors by world X position made this concrete — the hood's stripe band covers roughly ±0.18 out of a ±0.88 half-width, but the roof's "stripe" region ran nearly to its own ±0.72 edge. No texture recolor can fix a per-panel UV authoring choice like that, so the roof is now re-split geometrically by world position into `RoofPaint_stripe` and `RoofPaint_gray` — reusing the hood's own real stripe offset in meters rather than inventing a new width, so the roof's stripe lines up with the hood's. A first attempt also tried to reproduce the hood's thin gray gap between its two stripes; the roof's triangles turned out too coarse to resolve a gap that thin without a jagged, off-boundary sliver, caught by zooming into the render, so the roof gets one merged stripe at the hood's outer edge instead — true to the hood's width, not to its internal seam.

For the background: the same shared daylight HDR that already supplied reflections is now also `scene.background`, heavily blurred (`backgroundBlurriness 0.55`) and dimmed (`backgroundIntensity 0.55`) so it reads as soft ambient light rather than a literal sky the car floats in, plus a cool rim light from behind to separate the car's shaded side from that backdrop, and a darker, bigger contact shadow so the car reads as sitting on something rather than floating. The poster image is regenerated directly from this same harness at the viewer's own default camera angle, rather than cropped from an old verification render, so it no longer crossfades into a visibly different scene once the model loads.

TypeScript, lint, `build:cloudflare` and all fourteen checks pass (`check-garage.mjs` now expects 23 materials, up from 21, for the two new roof primitives). Worker version `13281798-2b12-43e3-98fc-eb4c2228eb0d`. Verified live this time by eye, not just by byte comparison: patched `document.hidden` on the real deployed page, scrolled to the Garage section (the nav link still doesn't work, per the known scroll-jack bug — got there by wheel-scrolling through the hero and past Flight log instead), and confirmed the roof now shows gray with continuing blue stripes, a visible soft backdrop and grounding shadow in place of the old void, and OrbitControls drag still rotating the car correctly. One pre-existing, unrelated cosmetic artifact noticed along the way and left alone: a thin bright yellow-green streak near the driver's door sill, present before any of this session's changes too, that raycasting traced to a specular highlight on the body-paint mesh rather than a texture or geometry bug — not what was asked, and not chased further.

## The roof stripe, narrowed to actually look like a stripe

Henok looked again and said the roof's stripe looked wrong next to the rest of the car. He was right: the fix above had merged the roof's twin stripe into one solid band at the same width as the hood's *pair* of stripes combined, so it read as a two-tone panel rather than a racing stripe — correct color, wrong proportion.

Narrowing it should have been trivial, but the first two attempts (0.13, then 0.095 world units, versus the shipped 0.19) both rendered a jagged zigzag instead of a clean edge, and — checked by direct triangle inspection, not by guessing — both attempts turned out to produce the *exact same* jagged shape regardless of the threshold. That ruled out "wrong number" and pointed at the classification method itself: the previous fix assigned whole triangles to a band by their centroid, and part of this one panel's own triangulation (traced with a raycaster and printed vertex-by-vertex) is a coarse, several-centimetre diagonal fan — any boundary that cuts through it lands mid-triangle for some of them, regardless of exactly where the boundary sits, since the fan's own triangles span a wide range of x on their own. The 0.19 in the previous fix wasn't a good width, it was just wide enough to clear the fan entirely.

The real fix is `split_at_plane`/`split_bands` in `prepare-garage-car.py`: proper Sutherland-Hodgman polygon clipping, cutting each straddling triangle exactly at the boundary plane and interpolating its position, normal and UV at the new vertex, rather than keeping or discarding it whole. This makes the edge exact regardless of the local mesh resolution, which meant two things became possible at once: a narrower stripe (0.11 vs. the hood's own 0.19 — the roof's flat top is narrower and more curved than the hood, so the same absolute width read as proportionally wider there, confirmed by directly measuring visible stripe width against panel width in a straight-down render), and reproducing the hood's thin gray pinstripe gap between its two stripes, which the previous fix had also abandoned as unrenderable on this mesh. Both were blocked by the whole-triangle approach, not by the geometry itself.

Verified the same way as the first roof fix and for the same reason (this session still can't get a real screen, so a standalone Playwright/three.js harness outside the site remains the fastest way to actually see a render): rendered straight down onto the roof at several widths, watched the zigzag persist identically across different thresholds until I actually inspected the raw triangle data instead of continuing to guess, then confirmed the fixed version gives a straight, gapped double stripe matching the hood's own proportions in both the top-down test view and the site's real default camera angle. TypeScript, lint, `build:cloudflare` and all fourteen checks still pass; the GLB grew by a few hundred triangles (126,246 → 126,636) from the clipping itself, expected and harmless. The poster image is regenerated again from the corrected model. Worker version `8f957a82-58d8-4e93-a720-f7bcbb732aec`. Verified live: patched `document.hidden` on the deployed page, scrolled to the Garage section (nav link still doesn't work, same pre-existing scroll-jack bug), and zoomed into the actual live roof — narrow, straight, gapped stripes, matching the hood.

## An actual road, then a sky that turned out not to be one

Henok asked for a cooler background — a realistic road. Source `61439c8`: a 40×80m asphalt plane (`roadTexture`, canvas-generated like everything else this viewer draws rather than fetches), speckled aggregate grain, two faint tire-wear paths, and two sun-faded lane edge lines a car's width apart, lit with `MeshStandardMaterial` so it actually responds to the scene's lights rather than sitting flat like the small contact-shadow disc still does. The grain aliased into a visible moiré at the shallow angle a ground plane is seen at; fixed by setting anisotropic filtering explicitly, which none of this viewer's other textures needed. Worker version `d986da56-c06d-46d1-85ec-78d785487f7c`.

He then asked to make the sky itself look like sky instead of the flat gray-purple it still read as. First attempt (`0e4228d`): the real daylight HDR's own `backgroundBlurriness` (0.55) and `backgroundIntensity` (0.55) were tuned down to 0.1/1.0, reasoning that the heavy blur was averaging in the hazy band near the horizon. That looked right in an isolated render and shipped (Worker `35dd4224-1ada-44e4-bb1d-2f3975ffc383`) — but looked exactly as gray live as before.

Chasing that down took most of this entry. The deployed JS was confirmed correct (fetched the live chunk directly and grepped it: `backgroundIntensity=1,backgroundBlurriness=.1`, byte-identical to source), the HDR fetch confirmed successful (200, not silently failing into the empty `.catch()`), and a from-scratch isolated test built to the exact same code, loaded in the same browser, produced numerically indistinguishable pixels from the live page at several matched sample points — ruling out a browser or GPU difference. What resolved it was cropping both to the actual viewer's aspect ratio and scanning a full vertical strip rather than eyeballing a thumbnail: both renders were flat gray from the top of the frame almost all the way to the horizon, no gradient, no blue, anywhere. The 0.1 blur "worked" in earlier renders only because those used a much higher camera looking down at a steeper angle; the real viewer's camera sits low, framing a parked car, and at its actual ~36° vertical FOV the only part of the HDR's dome ever in shot is the hazy band right at the horizon — the real blue sky in that file exists, just higher up in the dome than this camera has ever pointed. No amount of blur or intensity tuning on that region changes what region it is.

Source `2f2265b`: the visible backdrop is now a small generated gradient (`skyTexture`), sampled from the aircraft tour's own `cruise-sky.jpg` for a consistent blue across the site, set directly on `scene.background` at scene setup rather than waiting on the HDR load. The real daylight HDR stays exactly where it was useful — `scene.environment`, for the car's reflections — just no longer doing double duty as a backdrop it was never suited for at this angle. The gradient is a plain 2D texture, not an environment map, so it won't rotate with the camera the way a true sky would; acceptable since the viewer only orbits at a fairly constant, shallow elevation around the car, not enough for a static backdrop to look wrong. Worker version `375723d2-7eb3-4fc9-83af-238ccd69c6a6`.

TypeScript, lint, `build:cloudflare` and all fourteen checks pass throughout. All three versions verified live: the road's lane lines and grain rendering correctly, and — the one that mattered — the sky genuinely reading as blue on the actual deployed page this time, checked by eye after the numeric groundwork, not before it. The poster image was regenerated after each of the three changes so it never crossfades into a different-looking scene once the model loads.

Henok separately mentioned he also has a 2017 GT350 (non-R). Noted, not acted on: a second car is a new source model and its own licensing/conversion pass, the same scope of work the GT350R itself took, not a same-session follow-on to a background tweak.
